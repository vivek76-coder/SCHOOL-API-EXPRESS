import type { MongoshBus } from '@mongosh/types';
import type { AutoEncryptionOptions } from 'mongodb';
type SharedObjectSuffix = 'so' | 'dylib' | 'dll';
export declare const SHARED_LIBRARY_SUFFIX: SharedObjectSuffix;
export type CryptSharedLibPath = NonNullable<NonNullable<AutoEncryptionOptions['extraOptions']>['cryptSharedLibPath']>;
export interface CryptLibraryPathResult {
    cryptSharedLibPath?: CryptSharedLibPath;
    expectedVersion?: {
        version: bigint;
        versionStr: string;
    };
}
export declare function getCryptLibraryPaths(bus?: MongoshBus | undefined, pretendProcessExecPathForTesting?: string | undefined): Promise<CryptLibraryPathResult>;
export {};
