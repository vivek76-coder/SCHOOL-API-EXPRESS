"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.compileSourceFile = compileSourceFile;
exports.inferCollectionNameFromFunctionCall = inferCollectionNameFromFunctionCall;
const typescript_1 = __importDefault(require("typescript"));
function compileSourceFile(code) {
    return typescript_1.default.createSourceFile('_initial_parsing.ts', code, typescript_1.default.ScriptTarget.Latest, true);
}
const QUERY_METHODS = ['aggregate', 'find', 'findOne'];
function inferCollectionNameFromFunctionCall(sourceFile) {
    function findAggregateCallExpression(node) {
        if (typescript_1.default.isCallExpression(node) &&
            typescript_1.default.isPropertyAccessExpression(node.expression)) {
            const methodAccess = node.expression;
            if (QUERY_METHODS.includes(methodAccess.name.getText())) {
                return node;
            }
        }
        return typescript_1.default.forEachChild(node, findAggregateCallExpression);
    }
    const aggregateCallExpression = findAggregateCallExpression(sourceFile);
    if (aggregateCallExpression) {
        const aggregateExpression = aggregateCallExpression.expression;
        const dbIdentifier = aggregateExpression.expression;
        if (typescript_1.default.isPropertyAccessExpression(dbIdentifier)) {
            return dbIdentifier.name.getText();
        }
    }
    return null;
}
//# sourceMappingURL=cdt-analyser.js.map